#pragma once

#include <iostream>
#include <vector>

std::vector<std::tuple<int, int>> mergeRanges(const std::vector<std::tuple<int, int>>& ranges);
void printRange(const std::vector<std::tuple<int, int>>& ranges);